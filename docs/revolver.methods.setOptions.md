# setOptions(options)

**Type**: `Function`, **Returns**: `Object` (Instance of Revolver)

Merge new options in with the options object.

```javascript
mySlider.setOptions({
  rotationSpeed: 3500
});
```
